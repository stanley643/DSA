/**
 * The process of allocating memory at the time of execution is called dynamic memory allocation.
 *
 * Heap is the segment of memory where dynamic memory allocation takes place.
 *
 * unlike stack where memory is allocated or deallocated ina defined order, heap 
 * is an ares of memory where memory is allocated or deallocated without any order or randomly.
 *
 * Built in functions for dynamic memory allocation include:
 * malloc()
 * calloc()
 * realloc()
 * free()
 */
